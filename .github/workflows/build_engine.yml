name: 1. Build Engine (Manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-engine:
    runs-on: windows-latest
    
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    # 1. Full Python als Spender laden
    - name: 🐍 Setup Full Python (Source)
      uses: actions/setup-python@v4
      with:
        python-version: '3.10.11' 
        architecture: 'x64'

    # 2. Embeddable Python laden
    - name: 🐍 Download Embeddable Python (Target)
      shell: cmd
      run: |
        curl -L -o python.zip https://www.python.org/ftp/python/3.10.11/python-3.10.11-embed-amd64.zip
        mkdir Engine
        tar -xf python.zip -C Engine
        del python.zip

    # 3. TRANSPLANTATION (Tkinter & DLLs)
    - name: 🏥 Tkinter & DLLs transplantieren
      shell: powershell
      run: |
        $source = (python -c "import sys; print(sys.prefix)")
        $dest = "Engine"
        
        # Ordner erstellen
        New-Item -ItemType Directory -Force -Path "$dest\Lib"
        New-Item -ItemType Directory -Force -Path "$dest\DLLs"

        # DLLs kopieren
        copy "$source\DLLs\_tkinter.pyd" "$dest\DLLs\"
        copy "$source\DLLs\tcl86t.dll" "$dest\"
        copy "$source\DLLs\tk86t.dll" "$dest\"
        
        # Tkinter Code
        Copy-Item -Path "$source\Lib\tkinter" -Destination "$dest\Lib\tkinter" -Recurse -Force
        # Tcl Grafiken
        Copy-Item -Path "$source\tcl" -Destination "$dest\tcl" -Recurse -Force

    # 4. CONFIGURATION FIX (.pth)
    - name: 🔧 Pfade konfigurieren (.pth Datei)
      working-directory: Engine
      shell: cmd
      run: |
        echo python310.zip> python310._pth
        echo .>> python310._pth
        echo Lib>> python310._pth
        echo DLLs>> python310._pth
        echo Lib\site-packages>> python310._pth
        echo import site>> python310._pth

    # 5. PIP VORBEREITEN
    - name: 🔧 Pip installieren
      working-directory: Engine
      shell: cmd
      run: |
        curl -L -o get-pip.py https://bootstrap.pypa.io/get-pip.py
        .\python.exe get-pip.py --no-warn-script-location
        del get-pip.py

    # 6. INSTALLATION MIT ZWANG (--target)
    - name: 📦 Schwere Pakete installieren (FORCE TARGET)
      working-directory: Engine
      shell: cmd
      run: |
        REM Wir zwingen pip, in unseren lokalen Ordner zu installieren!
        set TARGET=Lib\site-packages
        if not exist "%TARGET%" mkdir "%TARGET%"

        echo Installiere PyTorch (GPU)...
        .\python.exe -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121 --target "%TARGET%" --no-user
        
        echo Installiere Requirements...
        .\python.exe -m pip install numpy==1.26.4 --target "%TARGET%" --no-user
        .\python.exe -m pip install TTS easyocr pyaudio keyboard pyautogui opencv-python pillow soundfile --target "%TARGET%" --no-user

        echo Bereinige Cache...
        .\python.exe -m pip cache purge
        
        REM Müll löschen um Platz zu sparen
        if exist "Lib\site-packages\torch\test" rmdir /s /q "Lib\site-packages\torch\test"
        if exist "Lib\site-packages\TTS\bin" rmdir /s /q "Lib\site-packages\TTS\bin"

    - name: 🤐 Engine Zippen (Split)
      shell: cmd
      run: |
        7z a -tzip -v1900m LQAG_Engine_v1.zip Engine

    - name: 🚀 Release erstellen
      uses: softprops/action-gh-release@v1
      with:
        tag_name: Engine-v1.4
        name: "LQAG Engine Base v1.4 (Fixed Install)"
        body: |
          ### WICHTIG:
          1. Lade **beide** Dateien (`.001` und `.002`).
          2. Rechtsklick auf `.001` -> 7-Zip -> Hier entpacken.
        draft: false
        prerelease: false
        files: LQAG_Engine_v1.zip.*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
