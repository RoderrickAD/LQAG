name: Build Engine (V13 - Version Lock)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-engine:
    runs-on: windows-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    # 1. MSVC Compiler
    - name: 🛠️ Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    # 2. System Python
    - name: 🐍 Setup System Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10.11' 
        architecture: 'x64'

    # 3. Portable Python
    - name: 🐍 Download Portable Python
      shell: cmd
      run: |
        curl -L -o python.zip https://www.python.org/ftp/python/3.10.11/python-3.10.11-embed-amd64.zip
        mkdir Engine
        tar -xf python.zip -C Engine
        del python.zip

    # 4. Header Transplant
    - name: 🧠 Header Transplantieren
      shell: powershell
      run: |
        $DonorPath = python -c "import sys; print(sys.prefix)"
        $Dest = "$pwd\Engine"
        
        # Tkinter
        New-Item -ItemType Directory -Force -Path "$Dest\Lib"
        New-Item -ItemType Directory -Force -Path "$Dest\DLLs"
        copy "$DonorPath\DLLs\_tkinter.pyd" "$Dest\DLLs\"
        copy "$DonorPath\DLLs\tcl86t.dll" "$Dest\"
        copy "$DonorPath\DLLs\tk86t.dll" "$Dest\"
        Copy-Item -Path "$DonorPath\Lib\tkinter" -Destination "$Dest\Lib\tkinter" -Recurse -Force
        Copy-Item -Path "$DonorPath\tcl" -Destination "$Dest\tcl" -Recurse -Force

        # C++ Header
        Copy-Item -Path "$DonorPath\include" -Destination "$Dest\include" -Recurse -Force
        Copy-Item -Path "$DonorPath\libs" -Destination "$Dest\libs" -Recurse -Force
        
        if (Test-Path "Engine\include\Python.h") { Write-Host "[OK] Header implantiert!" }
        else { Write-Host "[FEHLER] Header fehlen!" -ForegroundColor Red; exit 1 }

    # 5. Config Patch
    - name: 🔧 Config Patch
      working-directory: Engine
      shell: cmd
      run: |
        echo python310.zip> python310._pth
        echo .>> python310._pth
        echo Lib>> python310._pth
        echo DLLs>> python310._pth
        echo Lib\site-packages>> python310._pth
        echo import site>> python310._pth

    # 6. Pip Setup
    - name: 🔧 Pip Setup
      working-directory: Engine
      shell: cmd
      run: |
        curl -L -o get-pip.py https://bootstrap.pypa.io/get-pip.py
        .\python.exe get-pip.py --no-warn-script-location
        del get-pip.py
        .\python.exe -m pip install --upgrade pip setuptools wheel

    # 7. Build Tools
    - name: 🔨 Build Tools
      working-directory: Engine
      shell: cmd
      run: |
        .\python.exe -m pip install "numpy<1.24" cython

    # 8. Abhängigkeiten (MIT FIX!)
    - name: 🧱 Abhängigkeiten
      working-directory: Engine
      shell: cmd
      run: |
        REM HIER IST DER FIX: Wir zwingen transformers auf 4.33.0!
        .\python.exe -m pip install "transformers==4.33.0" scipy pandas librosa matplotlib pysbd tqdm coqpit trainer
        
        .\python.exe -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121 --no-warn-script-location

    # 9. TTS
    - name: 🗣️ Install TTS
      working-directory: Engine
      shell: cmd
      run: |
        .\python.exe -m pip install TTS==0.22.0 --no-build-isolation --no-warn-script-location

    # 10. Tools
    - name: 📦 Tools Installieren
      working-directory: Engine
      shell: cmd
      run: |
        .\python.exe -m pip install "opencv-python-headless<4.10" "opencv-python<4.10"
        .\python.exe -m pip install easyocr keyboard pyautogui pillow soundfile sounddevice --no-warn-script-location

    # 11. Tests
    - name: 🔍 FINALER SYSTEM-TEST
      working-directory: Engine
      shell: cmd
      run: |
        echo --- TEST KEYBOARD ---
        .\python.exe -c "import keyboard; print('Keyboard: OK')" || exit 1
        
        echo --- TEST TTS ---
        .\python.exe -c "import TTS; print('TTS: OK')" || exit 1
        
        echo --- TEST TRANSFORMERS ---
        .\python.exe -c "import transformers; print(f'Transformers: {transformers.__version__}')" || exit 1

    # 12. Zip
    - name: 🧹 Zippen
      working-directory: Engine
      shell: cmd
      run: |
        .\python.exe -m pip cache purge
        rmdir /s /q include
        rmdir /s /q libs
        if exist "Lib\site-packages\torch\test" rmdir /s /q "Lib\site-packages\torch\test"
        
        cd ..
        7z a -tzip -v1900m LQAG_Engine_V13.zip Engine

    - name: 🚀 Release Upload
      uses: softprops/action-gh-release@v1
      with:
        tag_name: Engine-V13-Stable
        name: "LQAG Engine V13 (Stable)"
        body: |
          ### Engine V13
          Transformers Version Lock (Fix für BeamSearchScorer).
          
          **Enthält:**
          - TTS 0.22.0
          - Transformers 4.33.0 (WICHTIG!)
          - Sounddevice
          - PyTorch
        draft: false
        prerelease: false
        files: LQAG_Engine_V13.zip.*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
